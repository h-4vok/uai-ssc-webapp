import React from 'react';
import { PlatformPageLayout } from '../organisms';

export const PlatformHomePage = props => (
  <PlatformPageLayout history={props.history}>
    <div styles="text:align: center;">Comience a operar</div>
  </PlatformPageLayout>
);
